# Public hostname that resolves to the EC2 Elastic IP. Example: brain.clinton-os.ca
DOMAIN=
ACME_EMAIL=clinton.reid@gmail.com
# openssl rand -base64 32
POSTGRES_PASSWORD=
# openssl rand -base64 32. Signs auth and every sync token. Never reuse the example.
JWT_SECRET=
# S3 bucket for nightly dumps, ca-central-1, created by deploy.sh
BACKUP_BUCKET=
# Optional. Blank keeps the semantic index air gapped.
OPENAI_API_KEY=
